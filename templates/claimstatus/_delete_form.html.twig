<form method="post" action="{{ path('app_claimstatus_delete', {'id': claimStatus.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ claimStatus.id) }}">
    <button class="btn">Удалить</button>
</form>
