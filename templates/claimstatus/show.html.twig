{% extends 'layout.html.twig' %}

{% block title %}Claim Status{% endblock %}

{% block body %}
    <h1>Claim Status</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ claimStatus.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ claimStatus.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_claimstatus_index') }}">back to list</a>

    <a href="{{ path('app_claimstatus_edit', {'id': claimStatus.id}) }}">edit</a>

    {{ include('claimstatus/_delete_form.html.twig') }}
{% endblock %}
